{
  "name": "form-server",
  "version": "0.0.0",
  "engines": {"node": ">=21"},
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "build": "typia generate --input src/ --output generated/",
    "dry-deploy": "wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "@types/html-escaper": "^3.0.2",
    "@types/node": "^20.14.2"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/helmet": "^11.1.1",
    "@fastify/multipart": "^8.2.0",
    "fastify": "^4.27.0",
    "form-types": "workspace:*",
    "html-escaper": "^3.0.3",
    "i18n-iso-countries": "^7.11.2",
    "typia": "^6.0.6"
  }
}
